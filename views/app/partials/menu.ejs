<!-- Sidenav Black Overlay-->
<div class="sidenav-black-overlay">


</div>
<!-- Side Nav Wrapper-->
<div class="suha-sidenav-wrapper" id="sidenavWrapper">
    <!-- Sidenav Profile-->
    <div class="sidenav-profile">

        <div class="user-profile"><img src="<%=((store.logo) ? `/media/${store.folder}/logo/logo180.webp?age=${store.logo.split('.')[0]}` : `/media/no-pic.jpg`)%>" alt></div>

        <div class="user-info">
            <h6 class="user-name mb-0">
                <%=store.name%>
            </h6>
            <p>
                <%=store.address%>
            </p>
            <p>
                <%=store.phone%>
            </p>

            <%if(store.bio){%>
                <p> <b>Bio</b><br>
                    <%=store.bio%>
                </p>
                <% }%>


        </div>
    </div>
    <!-- Night Mode View Card-->
    <!--<div class="night-mode-view-card">
        <div class="container">
            <div class="card settings-card">
                <div class="card-body">
                    <div class="single-settings d-flex align-items-center justify-content-between">
                        <div class="title"><i class="lni lni-night"></i><span>Modo Noturno</span></div>
                        <div class="data-content">
                            <div class="toggle-button-cover">
                                <div class="button r">
                                    <input class="checkbox" id="darkSwitch" type="checkbox">
                                    <div class="knobs"></div>
                                    <div class="layer"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>-->
    <!-- Sidenav Nav-->
    <ul class="sidenav-nav pl-0">
        <%if(store.instagram){%>
            <li></li> <%-((store.instagram) ? `<svg class="icon h21"><use xlink:href="#instagram"></use> </svg> ${store.instagram} <a target="_blanck" href="https://instagram.com/${store.instagram}"><span class="badge badge-info"">SEGUIR</span></a>` : '')%>
                 
        </li>
            <% }%>
                <%if(store.facebook){%>
                    <li><a href="https://www.facebook.com/<%=store.facebook%>"><i
                    class="lni lni-facebook"></i><%=store.facebook%></a></li>
                    <% }%>

    </ul>
    <!-- Go Back Button-->
    <div class="go-home-btn" id="goHomeBtn">X</div>
</div>